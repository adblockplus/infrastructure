/var/log/nginx/<%= @log %> {
  daily
  rotate 30
  compress
  delaycompress
  missingok
  dateext
  dateyesterday
  dateformat .%Y-%m-%d
  sharedscripts
  postrotate
    [ ! -f /var/run/nginx.pid ] || kill -USR1 `cat /var/run/nginx.pid`
  endscript
}

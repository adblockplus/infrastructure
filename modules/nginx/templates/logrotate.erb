/var/log/nginx/<%= @log %> {
  daily
  rotate 30
  compress
  delaycompress
  missingok
  nodateext
  sharedscripts
  postrotate
    [ ! -f /var/run/nginx.pid ] || kill -USR1 `cat /var/run/nginx.pid`
  endscript
}
